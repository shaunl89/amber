<h1>Meeting rooms</h1>

<table>
  <% @rooms.each do |room| %>
    <tr>
      <td><%= room.name %></td>
      <td><%= link_to 'Bookings', room_bookings_path(room) %></td>
    </tr>
  <% end %>
</table>
